<nav class="navbar navbar-expand-lg topbar">
  <div class="container-fluid px-4">
    <a class="navbar-brand brand-mark" href="<%= currentUser ? '/dashboard' : '/auth/login' %>">
      <span class="brand-icon"><i class="bi bi-journal-check"></i></span>
      ScholarTrack
    </a>
    <% if (currentUser) { %>
      <div class="d-flex align-items-center gap-3 ms-auto">
        <span class="user-chip d-none d-sm-inline-flex"><i class="bi bi-person-circle"></i> <%= currentUser.name %></span>
        <form action="/auth/logout" method="POST" class="m-0">
          <button class="btn btn-sm btn-outline-light" type="submit"><i class="bi bi-box-arrow-right"></i> Logout</button>
        </form>
      </div>
    <% } %>
  </div>
</nav>
